import React from 'react';
import {View, Text} from 'react-native';
import {Button} from 'react-native-paper';

const ReserveItem = ({navigation}) => {
  return (
    <View>
      <Text>ReserveItem</Text>
    </View>
  );
};

export default ReserveItem;
